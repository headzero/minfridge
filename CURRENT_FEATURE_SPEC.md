# 하루한칸 현재 기능명세

## 1. 서비스 개요

`하루한칸`은 냉장고 재고를 기록하고, 현재 보유 중인 재료를 기반으로 오늘 먹을 수 있는 요리를 추천해 주는 앱이다.

현재 구현 기준으로는 아래 범위를 포함한다.

- 로컬 저장 기반 비회원 사용
- Firebase 기반 사용자 동기화
- 다중 냉장고 관리
- 재고 등록/수정/비활성화
- 오늘의 추천 생성 및 재생성
- 추천 히스토리 확인
- 로컬 스케줄 기반 사전생성 및 알림

현재 추천 엔진은 실제 LLM API가 아니라 `RecipeRepository` 추상화 위의 mock 데이터로 동작한다.

## 2. 핵심 도메인

### 2.1 냉장고

- 사용자는 여러 개의 냉장고를 가질 수 있다.
- 냉장고는 `id`, `name`, `createdAt`, `updatedAt`, `isDefault`를 가진다.
- 최소 1개의 냉장고는 항상 유지되어야 한다.

### 2.2 재고 항목

- 재고 항목은 `식재료` 또는 `반찬` 유형을 가진다.
- 재고 항목은 다음 속성을 가진다.
  - `id`
  - `fridgeId`
  - `name`
  - `type`
  - `quantity`
  - `startedAt`
  - `createdAt`
  - `updatedAt`
  - `isActive`
- `quantity`는 1 이상의 정수이다.
- 삭제는 실제 삭제가 아니라 비활성 처리이며, 활성 항목만 화면에 표시된다.

### 2.3 오늘의 추천

- 추천은 날짜별 1세트로 관리된다.
- 각 추천은 다음 구조를 가진다.
  - `아침 3개`
  - `점심 3개`
  - `저녁 3개`
- 추천 상태는 성공 또는 실패로 관리된다.

## 3. 화면별 기능명세

## 3.1 홈 화면

- 앱 이름 표시
- 현재 선택된 냉장고 기준 재고 표시
- 냉장고 탭 전환 지원
- 냉장고 관리 진입 버튼 제공
- 냉장고 점유율 게이지 표시
- 재고 총수량 표시
- 재고 목록 표시
- 식재료 추가 버튼 제공
- 오늘 추천 화면 이동 버튼 제공
- 하단 배너 광고 영역 고정 노출

### 홈 화면 상세 동작

- 재고 총수량은 항목 개수가 아니라 `quantity` 합계 기준으로 계산한다.
- 점유율 게이지도 `quantity` 총합 기준으로 계산한다.
- 현재 구현상 기준 용량은 `30`이며, `총수량 / 30`으로 진행률을 계산한다.
- 점유율이 2/3를 넘으면 경고 색상과 경고 문구를 표시한다.

## 3.2 오늘 추천 화면

- 오늘의 추천 제목 표시
- 당일 수동 새로고침 잔여 횟수 표시
- 수동 새로고침 버튼 제공
- 오늘 추천 조회 버튼 제공
- 추천 성공 시 아침/점심/저녁 카드 표시
- 추천 실패 시 실패 카드 표시
- 하단 배너 광고 영역 고정 노출

### 오늘 추천 상세 동작

- 추천이 성공하면 각 식사 구간별 추천 목록을 보여준다.
- 추천이 없거나 실패 상태이면 실패 카드와 재시도 유도 문구를 표시한다.
- 수동 새로고침은 일일 횟수 제한 정책을 따른다.

## 3.3 추천 히스토리 화면

- 날짜별 추천 기록 목록 제공
- 최신 날짜가 상단에 오도록 정렬
- 성공한 경우 식사별 추천 개수 요약 표시
- 실패한 경우 `생성 실패` 상태 표시

## 3.4 설정 화면

- 알림 사용 상태 안내
- 현재 사용자 UID 표시
- Google 로그인 버튼 제공
- Apple 로그인 버튼 제공
- 비회원 전환 버튼 제공
- 병합 정책 안내 문구 표시
- 최근 7일 좋아요 비율 표시

## 4. 냉장고 관리 기능

### 4.1 냉장고 추가

- 사용자는 새 냉장고 이름을 입력해 냉장고를 추가할 수 있다.
- 냉장고 추가 시 자동으로 선택 가능한 목록에 반영된다.

### 4.2 냉장고 이름 변경

- 사용자는 기존 냉장고 이름을 변경할 수 있다.

### 4.3 냉장고 삭제

- 마지막 1개 냉장고는 삭제할 수 없다.
- 삭제 대상 냉장고에 재고가 있는 경우, 현재 구현상 다른 냉장고로 이동 후 삭제한다.
- 삭제 후 현재 선택 냉장고가 사라지면 첫 번째 냉장고가 선택된다.

## 5. 재고 관리 기능

### 5.1 재고 추가

- 사용자는 다음 값을 입력해 재고를 추가할 수 있다.
  - 이름
  - 수량
  - 유형(식재료/반찬)
  - 보관 시작일
- 수량이 1 미만이면 저장하지 않는다.

### 5.2 재고 수정

- 기존 재고의 이름, 수량, 유형, 보관 시작일을 수정할 수 있다.

### 5.3 재고 삭제

- 삭제 시 실제 제거 대신 비활성 처리한다.
- 삭제 사유는 `소진` 또는 `폐기` 중 하나를 선택한다.

### 5.4 재고 목록 표시

- 선택된 냉장고의 활성 재고만 표시한다.
- 각 항목에는 다음 정보가 표시된다.
  - 이름
  - 수량
  - 보관 일수
- 보관 일수 기준 배경색 구간은 아래와 같다.
  - 0~3일
  - 4~7일
  - 8~14일
  - 15~28일
  - 29일 이상

## 6. 추천 기능

### 6.1 추천 생성

- 추천 생성은 `RecipeRepository`를 통해 수행한다.
- 현재 구현은 `MockRecipeRepository`를 사용한다.
- 추천 생성 입력값은 다음과 같다.
  - 날짜 키
  - 현재 선택 냉장고의 활성 재고 목록
  - 상위 품질 모델 사용 여부

### 6.2 자동 생성

- 앱 부팅 시 당일 추천 사전생성 조건을 확인한다.
- 해당 시간대와 슬롯 조건을 만족하면 오늘 추천을 자동 생성한다.

### 6.3 수동 재생성

- 사용자는 오늘 추천 화면에서 수동 새로고침을 실행할 수 있다.
- 기본 일일 최대 시도 횟수는 3회다.
- 3회 모두 실패했고 당일 성공 추천이 한 번도 없는 경우에만 10분 차단 후 재시도 가능하다.
- 자정(00:00) 이후 일일 카운트가 초기화된다.

### 6.4 품질 보정 플래그

- 최근 7일 좋아요 비율이 40% 미만이고,
- 오늘 추천의 총 추천 수가 3개 미만인 경우,
- 상위 품질 모델 재시도 플래그를 활성화하도록 설계되어 있다.
- 현재는 실제 모델 분기 대신 repository 호출 파라미터만 존재한다.

## 7. 추천 히스토리 및 만족도

### 7.1 히스토리 보관

- 추천 결과는 날짜 키 기준으로 메모리 상태에 누적 보관된다.
- 히스토리 화면에서 목록 형태로 열람할 수 있다.

### 7.2 만족도 수집

- 사용자가 홈 화면에서 백버튼으로 앱 종료를 시도할 때만 만족도 다이얼로그를 표시한다.
- 선택지는 다음과 같다.
  - 좋아요
  - 싫어요
  - 건너뛰기
- 선택값은 최근 7일 좋아요 비율 계산에 사용된다.

## 8. 로그인 및 사용자 식별

### 8.1 비회원 사용

- 앱 최초 실행 시 Firebase 익명 로그인으로 비회원 사용이 가능하다.
- 비회원 상태에서도 재고 관리와 추천 기능을 사용할 수 있다.

### 8.2 Google 로그인

- Google 계정 로그인 지원
- 현재 익명 사용자 상태라면 기존 세션에 계정을 링크한다.

### 8.3 Apple 로그인

- Apple 로그인 코드 경로 포함
- 현재 프로젝트 기준으로 버튼 및 로그인 흐름이 연결되어 있다.

### 8.4 비회원 전환

- 사용자는 로그아웃 후 다시 익명 사용자 상태로 전환할 수 있다.

## 9. 로컬 저장 및 동기화

### 9.1 로컬 저장

- 앱 상태는 로컬 스냅샷으로 저장된다.
- 저장 대상은 다음을 포함한다.
  - 냉장고 목록
  - 재고 목록
  - 최종 업데이트 시각

### 9.2 Firebase Realtime Database 동기화

- 사용자 데이터는 `users/{uid}` 경로에 저장된다.
- 저장 대상은 다음을 포함한다.
  - `fridges`
  - `items`
  - `meta.localUpdatedAt`
  - `meta.serverUpdatedAt`

### 9.3 앱 시작 시 자동 동기화

- 클라우드 데이터가 없으면 로컬 데이터를 업로드한다.
- 클라우드가 더 최신이면 클라우드 데이터를 pull 한다.
- 로컬이 더 최신이면 로컬 데이터를 push 한다.
- 동일하면 변경 없음으로 처리한다.

### 9.4 로그인 후 병합 정책

- 클라우드 데이터가 없으면 로컬을 자동 업로드한다.
- 클라우드 데이터가 있으면 병합 여부를 묻는다.
- 사용 가능한 선택지는 다음과 같다.
  - 클라우드 사용
  - 로컬 업로드
  - 최신 기준 병합

### 9.5 최신 기준 병합

- 냉장고와 재고 각각을 `id` 기준으로 병합한다.
- 동일 `id`가 존재하면 `updatedAt`이 더 최신인 데이터를 채택한다.

### 9.6 네트워크 실패 대응

- Firebase 동기화는 재시도와 백오프를 적용한다.
- 재시도 대상은 timeout 및 일부 네트워크 오류다.

## 10. 알림 및 스케줄

### 10.1 사전생성 분산 정책

- 오늘 추천 사전생성 시간대는 사용자 로컬 시간 기준 `00:00~06:00`이다.
- 각 사용자는 `uid + dateKey` 해시 기반 슬롯으로 분산된다.
- 같은 시간에 모든 사용자가 동시에 추천을 생성하지 않도록 설계되어 있다.

### 10.2 오전 알림

- 매일 오전 07:00에 오늘의 추천 알림을 보낸다.
- 해당 시점에 추천이 존재하면 준비 완료 알림을 발송한다.
- 추천이 없거나 생성 실패면 재생성 시도 후 실패 알림을 발송한다.

### 10.3 현재 구현상의 제약

- 현재 스케줄은 앱 내부 `Timer` 기반이다.
- 따라서 앱이 완전히 종료된 상태에서 OS 레벨 백그라운드 스케줄이 보장되지는 않는다.
- 현재 MVP 기준에서는 앱이 실행 중이거나 재개될 때 중심으로 동작한다.

## 11. 광고

- 홈 화면과 오늘 추천 화면 하단에 배너 광고 영역 placeholder가 항상 노출된다.
- 현재는 실제 광고 SDK가 아니라 UI placeholder 상태다.

## 12. 현재 구현 기준 비포함 또는 향후 작업

- 실제 LLM API 연동
- 추천 품질 피드백의 서버 분석
- OS 레벨 백그라운드 스케줄 보장
- 실제 광고 SDK 연동
- 히스토리 장기 보존 정책의 영속 저장 고도화
- 튜토리얼, 온보딩, 상세 레시피 화면

