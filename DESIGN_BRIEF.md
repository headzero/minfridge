# 하루한칸(MINFRIDGE) — Claude Design 요청 브리프

> 이 문서는 현재 구현된 Flutter 앱(`lib/src/ui/home_shell.dart` 중심)을 디자인 리뉴얼하기 위해
> Claude Design에 전달하는 사양서다. 현재 앱은 기능 위주로만 구현되어 있고 **커스텀 테마가 없는
> 기본 Material 3 위젯** 상태다. 새 디자인 시스템(색상·타이포·스페이싱 토큰)과 화면별 비주얼을 요청한다.

---

## 0. 작업 요청 요약 (Claude Design이 산출할 것)

1. **디자인 시스템**: 컬러 팔레트(라이트/다크), 타이포 스케일, 스페이싱/라운드/엘리베이션 토큰
2. **화면 4종 + 모달/다이얼로그 5종**의 리디자인 (아래 3~4장 참조)
3. **상태별 디자인**: 빈 상태, 임박 경고, 점유율 초과 경고, 추천 실패, 로딩/처리중
4. **컴포넌트 사양**: 재고 카드, 점유율 게이지, 식사 추천 카드, 냉장고 탭칩, 배너 광고 영역
5. **핸드오프**: 위 결과를 "Send to Claude Code"로 spec 번들 전달 (→ Flutter/Material3로 구현)

**기술 제약**: Flutter + Material 3, 상태관리 provider. 모든 텍스트 **한국어**. 모바일 세로 화면 우선.

---

## 1. 제품 개요 & 톤

- **이름**: 하루한칸 (MINFRIDGE)
- **한 줄 정의**: 냉장고 재고를 기록하고, 보유 재료로 오늘 먹을 끼니(아침/점심/저녁)를 추천받는 앱
- **핵심 사용자 가치**: ① 냉장고에 뭐가 있는지/유통기한 ② 그걸로 뭘 해먹지
- **톤 제안(검토 요청)**: 따뜻하고 생활밀착형, 식재료의 신선도를 직관적으로 보여주는 신호등 색감.
  과하지 않고 1인 가구가 매일 부담 없이 여는 느낌.

---

## 2. 정보구조 / 내비게이션

하단 `NavigationBar` 4탭 (현재 아이콘 → 개선 검토):
| 탭 | 라벨 | 현재 아이콘 |
|---|---|---|
| 0 | 홈 | `kitchen_outlined` |
| 1 | 오늘 추천 | `restaurant_menu` |
| 2 | 히스토리 | `history` |
| 3 | 설정 | `settings` |

- 홈에서 **안드로이드 백버튼**으로 앱 종료 시 → "오늘 추천 만족도" 다이얼로그 노출(좋아요/싫어요/건너뛰기).
- 홈/오늘추천 화면 **하단에 56px 배너 광고 영역이 항상 고정 노출**(현재 placeholder).

---

## 3. 화면별 사양

### 3.1 홈 (재고)
구성 요소(위→아래):
1. 헤더: 앱명 "하루한칸" + 우측 "냉장고 관리" 아이콘 버튼(`tune`)
2. **냉장고 탭**: 가로 스크롤 ChoiceChip 목록(선택 1개 강조). 사용자는 여러 냉장고 보유.
3. **점유율 게이지 카드** (아래 4.2 참조)
4. 액션 버튼 2개: `식재료 추가`(채움형), `오늘 추천 보기`(외곽선형)
5. 재고 헤더: `재고 (총수량)` — **항목 개수가 아니라 수량(quantity) 합계**
6. **재고 카드 리스트** (아래 4.1 참조)
7. 배너 광고 영역

빈 상태(재고 0개) 디자인 필요: "첫 재료를 추가해보세요" 류의 안내 + CTA.

### 3.2 오늘 추천
1. 제목 "오늘의 추천"
2. `수동 새로고침 남은 횟수: N회` (하루 최대 3회 정책)
3. 액션: `수동 새로고침`(채움형, 처리중 표시 필요), `오늘 조회`(외곽선형)
4. **성공 시**: 아침/점심/저녁 3개 섹션 카드. 각 섹션은 제목 + 3개 메뉴 불릿 리스트.
5. **실패/없음 시**: 실패 카드("추천 생성 실패", 실패 횟수, 재시도 유도 문구)
6. 배너 광고 영역

→ 성공/실패/로딩 3가지 상태 모두 디자인 필요.

### 3.3 히스토리
- 제목 "추천 히스토리 (1년)"
- 날짜별 카드 리스트(최신 상단). 성공: `아침 N / 점심 N / 저녁 N` 요약. 실패: `생성 실패` 상태.
- 빈 상태 디자인 필요.

### 3.4 설정
- 알림 권한 안내
- 사용자 UID 표시
- 로그인 상태(`회원 로그인됨`/`비회원 사용 중`) + 오류 메시지 영역
- 버튼: `Google 로그인`, `Apple 로그인`, `비회원으로 전환` (처리중 비활성 상태 필요)
- 병합 정책 안내 문구
- 최근 7일 좋아요 비율 (%)

---

## 4. 핵심 컴포넌트 (현재 로직 — 디자인이 이 의미를 반영해야 함)

### 4.1 재고 카드 (`_FoodTile`)
- 표시: **이름**, 서브텍스트 `수량 N개 · 보관 D일 · 유통기한 D-n (예상)`, 우측 수정/삭제 아이콘
- **카드 배경색 = 신선도 신호**. 우선순위:
  - 유통기한이 있으면 **남은 일수 기준**:
    | 남은 일수 | 의미 | 현재 색 |
    |---|---|---|
    | ≤1일 (또는 지남) | 위급 | `#FFCDD2` (빨강) |
    | ≤3일 | 임박 | `#FFE0B2` (주황) |
    | ≤7일 | 주의 | `#FFFDE7` (옅은 노랑) |
    | 그 외 | 여유 | `#E8F5E9` (옅은 초록) |
  - 유통기한이 없으면 **보관 일수 기준**: 0~3 `#E8F5E9` / 4~7 `#FFFDE7` / 8~14 `#FFF3E0` / 15~28 `#FFE0B2` / 29+ `#FFCDD2`
- **"예상" 뱃지**: 유통기한이 자동 추정값일 때 표시(사용자 직접 입력이면 미표시). 신뢰도 구분 시각화 요청.
- D-day 라벨 표기: `D-3`, `D-day`, `지남`.

> 디자인 요청: 위 신호등 의미를 유지하되, 카드 전체 배경 채색 대신 **상태 인디케이터(좌측 바/칩/도트 등)**
> 로 더 세련되게 표현하는 안도 제안해 달라. 색상 토큰은 새 팔레트에 맞춰 재정의 가능.

### 4.2 점유율 게이지 카드 (`_GaugeCard`)
- `냉장고 점유율 N%` + 가로 막대(LinearProgressIndicator) + 상태 문구
- 계산: 기준 용량 30, 진행률 = 총수량 / 30 (0~1 clamp)
- **2/3(0.66) 초과 시 경고**: 막대 색 `deepOrange`, 문구 "주의: 목표(2/3) 초과". 이내면 `teal`, "목표 이내".
- 디자인 요청: 정상/경고 2상태 명확히. 게이지를 더 매력적으로(원형/세그먼트 등 대안 제안 환영).

### 4.3 식사 추천 카드 (`_MealSection`)
- 제목(아침/점심/저녁) + 메뉴 불릿 3개. 끼니별 아이콘/색 구분 제안 환영.

### 4.4 냉장고 탭칩 / 배너 광고 영역
- 탭칩: 선택/비선택 상태. 배너: 56px 고정, "광고 영역" placeholder 비주얼.

---

## 5. 모달 / 다이얼로그

### 5.1 재고 추가·수정 (바텀시트, 키보드 회피 스크롤)
입력 필드 순서:
1. 이름 (텍스트)
2. 수량 (숫자, 1 이상 — 미만이면 저장 불가)
3. 유형 (드롭다운: **식재료 / 반찬**)
4. 보관 시작일 (날짜 선택)
5. **유통기한 (선택)**: 기본은 `(예상·선택)` — 이름/유형/시작일로 **자동 추정값을 실시간 미리보기**.
   `날짜 선택` 누르면 직접 입력(`(직접 입력)`로 라벨 변경), 직접 입력 후 `예상값` 버튼으로 되돌리기 가능.
6. 저장 버튼
→ 이 "선택이지만 추정값이 미리 채워지는" UX가 핵심. 부담 없이 비워도 되고, 원하면 보정하는 인터랙션을 잘 보여줄 것.

### 5.2 삭제 사유 다이얼로그
- `소진` / `폐기` 2선택.

### 5.3 냉장고 관리 (바텀시트)
- 냉장고 목록: 각 항목 이름 + 현재 선택 표시 + 이름변경/삭제 아이콘.
- 하단: 새 냉장고 이름 입력 + 추가 버튼.
- 제약: 마지막 1개는 삭제 불가(스낵바 안내).

### 5.4 데이터 병합 선택 다이얼로그 (로그인 후)
- 로컬/클라우드 업데이트 시각 비교 안내 + 3선택: `클라우드 사용` / `로컬 업로드` / `최신 기준 병합`.

### 5.5 만족도 다이얼로그 (홈 백버튼 종료 시)
- "오늘 추천이 도움이 되었나요?" + `좋아요` / `싫어요` / `건너뛰기`.

---

## 6. 디자인 시스템에 바라는 것

- **컬러**: 브랜드 컬러(현재 임시로 teal/deepOrange 사용 중) 재정의 + 신선도 신호등(여유/주의/임박/위급) 시맨틱 컬러 세트. 라이트 & **다크 모드** 둘 다.
- **타이포**: 한국어 가독성 우선. 화면 제목(현재 22/bold), 섹션 헤더, 본문, 캡션 스케일.
- **형태**: 카드 라운드/엘리베이션, 버튼(채움/외곽선/텍스트) 스타일, 칩, 인풋.
- **아이콘**: 하단 탭/액션 아이콘 일관성.
- **접근성**: 색만으로 상태를 구분하지 않도록(라벨/아이콘 병행), 명도 대비 확보.

---

## 7. 범위 밖 / 주의

- 실제 광고 SDK·LLM 추천·바코드 스캔은 아직 미구현(placeholder/mock). 디자인엔 자리만 반영.
- 데이터 모델은 이미 `quantity`, `expiresAt(+예상 출처)`까지 구현됨 — 디자인은 이 정보를 노출하는 방향으로.
- 구현 타깃은 Flutter Material 3이므로, 핸드오프 spec은 Material 컴포넌트로 매핑 가능한 수준으로.

---

## 8. 핸드오프 절차 (Claude Design → 이 레포)

1. Claude Design에 이 레포(`minfridge`)를 **링크**하고 본 문서를 참조로 첨부.
2. 디자인 완료 후 **"Send to Claude Code"**로 spec 번들 생성.
3. 번들/spec 파일을 이 레포에 두거나 경로를 공유 → Claude Code가 `lib/src/ui/` 리뉴얼 구현.
